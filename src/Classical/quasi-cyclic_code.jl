# Copyright (c) 2022 - 2026 Eric Sabo
# All rights reserved.
#
# This source code is licensed under the BSD-style license found in the
# LICENSE file in the root directory of this source tree.

#############################
        # Helpers
#############################

function _poly_to_circulant(poly, m::Int, F)
    C_mat = zero_matrix(F, m, m)
    iszero(poly) && return C_mat
    
    for r in 1:m
        for c in 1:m
            # Right-shift circulant: Row 1 gets coeff(c-1).
            # Subsequent rows right-shift the previous row.
            idx = mod(c - r, m)
            C_mat[r, c] = F(coeff(poly, idx))
        end
    end
    return C_mat
end

function _expand_qc_matrix(A::MatElem, m::Int, l::Int, F)
    nr, nc = size(A) 
    M_expanded = zero_matrix(F, nr * m, nc * m)
    for r in 1:nr
        for c in 1:nc
            circ = _poly_to_circulant(lift(A[r, c]), m, F)
            # The (i, j)-th entry of the circulant goes to row (r-1)*m + i
            # and column (j-1)*l + c (interleaved representation).
            for i in 1:m
                for j in 1:m
                    M_expanded[(r - 1) * m + i, (j - 1) * l + c] = circ[i, j]
                end
            end
        end
    end
    return M_expanded
end

#############################
        # constructors
#############################

function generator_matrix(C::QuasiCyclicCode, stand_form::Bool = false)
    cache = getfield(C, :cache)
    if C.A_type == :G && !haskey(cache, :G)
        cache[:G] = noncirculant_generator_matrix(C)
    elseif C.A_type == :H && !haskey(cache, :G)
        H_lift = parity_check_matrix(C)
        _, G = right_kernel(H_lift)
        cache[:G] = transpose(G)
    end
    
    if stand_form
        if !haskey(cache, :G_stand)
            G_stand, H_stand, P, _ = _standard_form(cache[:G])
            cache[:G_stand] = G_stand
            cache[:H_stand] = H_stand
            cache[:P_stand] = P
        end
        return cache[:G_stand]
    end
    return cache[:G]
end

function parity_check_matrix(C::QuasiCyclicCode, stand_form::Bool = false)
    cache = getfield(C, :cache)
    if C.A_type == :H && !haskey(cache, :H)
        cache[:H] = noncirculant_parity_check_matrix(C)
    elseif C.A_type == :G && !haskey(cache, :H)
        G_lift = generator_matrix(C)
        H = kernel(G_lift, side = :right)
        cache[:H] = transpose(H)
    end
    
    if stand_form
        generator_matrix(C, true)
        return cache[:H_stand]
    end
    return cache[:H]
end

"""
$(TYPEDSIGNATURES)

Return the quasi-cyclic code specified by the matrix `A` of polynomial circulant generators.
Evaluates lazily.
"""
function QuasiCyclicCode(A::MatElem{T}, parity::Bool=false) where T <: ResElem
    R = parent(A[1, 1])
    S = base_ring(R)
    F = base_ring(S)
    g = modulus(R)
    m = degree(g)
    g == gen(S)^m - 1 || throw(ArgumentError("Residue ring not of the form x^m - 1."))
    
    l = ncols(A)
    nr = nrows(A)
    n_full = l * m
    
    # Fully expand the matrix over F to cleanly evaluate true rank
    M_expanded = _expand_qc_matrix(A, m, l, F)
    rnk = rank(M_expanded)
    
    k = parity ? (n_full - rnk) : rnk
    A_type = parity ? :H : :G
    
    cache = Dict{Symbol, Any}()
    if A_type == :G
        cache[:G] = M_expanded
    else
        cache[:H] = M_expanded
    end
    
    return QuasiCyclicCode(F, R, n_full, k, missing, 1, n_full, l, m, A, A_type, cache)
end

"""
    QuasiCyclicCode(v::Vector{fqPolyRepMatrix}, l::Int, circ_gens::Bool, parity::Bool=false)

Return the quasi-cyclic code of index `l` generated by right-bit shifts of size `l` of the
generator vectors `v`. If `circ_gens` is `true`, the vectors are taken to be (column) generators
for the circulant matrices instead of generator vectors for the code.
If the optional paramater `parity` is set to `true`, the input is used to construct the parity-check matrix.
# Notes
* If `circ_gens` is `false`, then the length of the code is `ncols(v[1])` and must be divisible by `l`.
* If `circ_gens` is `true`, then the length of the code is `ncols(v[1]) * l`. Circulant matrices are
  stacked in rows of length `l`, so `l` must divide `length(v)`.
"""
function QuasiCyclicCode(v::Vector{T}, l::Int, circ_gens::Bool, parity::Bool=false) where T <: CTMatrixTypes
    F = base_ring(v[1])
    len_v = length(v)
    if circ_gens
        len_v >= 2 || throw(ArgumentError("Length of input vector must be at least two."))
        len_v % l == 0 || throw(ArgumentError("The length of the input vector must be divisible by l."))
        nr = div(len_v, l)
        r, m = size(v[1])
        (r != 1 && m != 1) && throw(ArgumentError("The input matrices must be vectors."))
        m == 1 && (v[1] = transpose(v[1]); (r, m = size(v[1]));)
        for i in 2:len_v
            F == base_ring(v[i]) || throw(ArgumentError("All inputs must be over the same base ring."))
            r2, m2 = size(v[i])
            (r2 != 1 && m2 != 1) && throw(ArgumentError("The input matrices must be vectors."))
            m2 == 1 && (v[i] = transpose(v[i]); (r2, m2 = size(v[i]));)
            m == m2 || throw(ArgumentError("The input vectors must all be the same length."))
        end

        S, x = polynomial_ring(F, :x)
        R, _ = residue_ring(S, x^m - 1)
        A = zero_matrix(R, nr, l)
        for r in 1:nr
            for c in 1:l
                temp = [v[(r - 1) * l + c][i] for i in 1:m]
                A[r, c] = R(S(temp))
            end
        end
        return QuasiCyclicCode(A, parity)
    else
        r, n = size(v[1])
        (r != 1 && n != 1) && throw(ArgumentError("The input matrices must be vectors."))
        n == 1 && (v[1] = transpose(v[1]); (r, n = size(v[1]));)
        n % l == 0 || throw(ArgumentError("Parameter l must divide the length of the vector."))
        m = div(n, l)
        for i in 2:len_v
            F == base_ring(v[i]) || throw(ArgumentError("All vectors must be over the same base ring."))
            r2, n2 = size(v[i])
            (r2 != 1 && n2 != 1) && throw(ArgumentError("The input matrices must be vectors."))
            n2 == 1 && (v[i] = transpose(v[i]); (r2, n2 = size(v[i]));)
            n == n2 || throw(ArgumentError("The input vectors must all be the same length."))
        end
        
        S, x = polynomial_ring(F, :x)
        R, _ = residue_ring(S, x^m - 1)
        A = zero_matrix(R, len_v, l)
        for k in 1:len_v
            for i in 1:l
                row = v[k]
                top_circ_row = zero_matrix(F, m, 1)
                for j in 1:m
                    top_circ_row[j, 1] = row[i + (j - 1) * l]
                end
                # transpose to get first circulant column
                top_circ_row[2:end, :] = top_circ_row[end:-1:2, :]
                A[k, i] = R(S([top_circ_row[i, 1] for i in 1:m]))
            end
        end
        return QuasiCyclicCode(A, parity)
    end
end

"""
    QuasiCyclicCode(v::fqPolyRepMatrix, l::Int, parity::Bool=false)

Return the quasi-cyclic code of index `l` generated by right-bit shifts of size `l` of the
generator vector `v`. If the optional paramater `parity` is set to `true`, the input is used
to construct the parity check matrix.
"""
QuasiCyclicCode(v::CTMatrixTypes, l::Int, parity::Bool=false) = QuasiCyclicCode([v], l, false, parity)

"""
    QuasiCyclicCode(v::Vector{fqPolyRepPolyRingElem}, n::Int, l::Int, parity::Bool=false)

Return the quasi-cyclic code of index `l` whose circulants are defined by the generator
polynomials `v`. If the optional paramater `parity` is set to `true`, the input is used
to construct the parity check matrix.
"""
function QuasiCyclicCode(v::Vector{T}, n::Int, l::Int, parity::Bool=false) where T <: CTMatrixTypes
    gen_vecs = Vector{T}()
    F = base_ring(v[1])
    for g in v
        temp = zero_matrix(F, 1, n)
        coeffs = collect(coefficients(g))
        temp[1, 1:length(coeffs)] = coeffs
        push!(gen_vecs, temp)
    end
    return QuasiCyclicCode(gen_vecs, l, true, parity)
end

"""
    QuasiCyclicCode(v::Vector{AbstractCyclicCode}, l::Int, parity::Bool=false)

Return the quasi-cyclic code of index `l` whose circulants are determined by the cyclic
codes in `v`. If the optional paramater `parity` is set to `true`, the input is used to
construct the parity check matrix.
"""
function QuasiCyclicCode(v::Vector{AbstractCyclicCode}, l::Int, parity::Bool=false)
    # Using generator_matrix to safely access fields
    gen_vecs = Vector{typeof(generator_matrix(v[1]))}()
    for C in v
        push!(gen_vecs, generator_matrix(C)[1:1, :])
    end
    return QuasiCyclicCode(gen_vecs, l, true, parity)
end

#############################
      # getter functions
#############################

"""
    index(C::AbstractQuasiCyclicCode)

Return the index of the quasi-cyclic code.
"""
index(C::AbstractQuasiCyclicCode) = C.l

"""
    expansion_factor(C::AbstractQuasiCyclicCode)

Return the expansion factor of the quasi-cycle code `C`.
"""
expansion_factor(C::AbstractQuasiCyclicCode) = C.m

"""
    type(C::AbstractQuasiCyclicCode)

Return the type of the quasi-cycle code `C`.
"""
type(C::AbstractQuasiCyclicCode) = C.type

"""
    polynomial_matrix(C::AbstractQuasiCyclicCode)

Return the polynomial matrix used to define the code.
Use `polynomial_matrix_type` to determine if specifies the generator or parity-check matrix.
"""
polynomial_matrix(C::AbstractQuasiCyclicCode) = C.A

"""
    polynomial_matrix_type(C::AbstractQuasiCyclicCode)

Return `'G'` if the polynomial matrix of `C` specifies the generator or parity-check matrix.
"""
polynomial_matrix_type(C::AbstractQuasiCyclicCode) = C.A_type

"""
    is_single_generator(C::AbstractQuasiCyclicCode)

Return `true` if `C` is a single-generator quasi-cyclic code.
"""
is_single_generator(C::AbstractQuasiCyclicCode) = (nrows(C.A) == 1;)

#############################
     # general functions
#############################

"""
    base_matrix(A::MatElem{T}) where T <: ResElem
    protograph_matrix(A::MatElem{T}) where T <: ResElem
    weight_matrix(A::MatElem{T}) where T <: ResElem

Return the base/protograph/weight matrix of `A`.
"""
function weight_matrix(A::MatElem{T}) where T <: ResElem
    nr, nc = size(A)
    W = zeros(Int, nr, nc)
    for c in 1:nc
        for r in 1:nr
            W[r, c] = wt(Nemo.lift(A[r, c]))
        end
    end
    return W
end
base_matrix(A::MatElem{T}) where T <: ResElem = weight_matrix(A)
protograph_matrix(A::MatElem{T}) where T <: ResElem = weight_matrix(A)

"""
    noncirculant_generator_matrix(C::AbstractQuasiCyclicCode)

Return the non-circulant form of the generator matrix for the quasi-cyclic code `C` 
if the polynomial matrix specifies the generator matrix; otherwise, return `missing`.
"""
function noncirculant_generator_matrix(C::AbstractQuasiCyclicCode)
    if C.A_type == :G
        return _expand_qc_matrix(C.A, C.m, C.l, C.F)
    else
        return missing
    end
end

"""
    noncirculant_parity_check_matrix(C::AbstractQuasiCyclicCode)

Return the non-circulant form of the parity-check matrix for the quasi-cyclic code `C`
if the polynomial matrix specifies the parity-check matrix; otherwise, return `missing`.
"""
function noncirculant_parity_check_matrix(C::AbstractQuasiCyclicCode)
    if C.A_type == :H
        return _expand_qc_matrix(C.A, C.m, C.l, C.F)
    else
        return missing
    end
end

"""
    generators(C::AbstractQuasiCyclicCode)

Return the generators of the quasi-cyclic code.
"""
function generators(C::AbstractQuasiCyclicCode)
    G = noncirculant_generator_matrix(C)
    gen_vecs = Vector{typeof(G)}()
    nr = nrows(G)
    for i in 1:nr
        # Extract the primary shift vector from each interleaved block
        if i % C.m == 1
            push!(gen_vecs, G[i:i, :])
        end
    end
    return gen_vecs
end

"""
    circulants(C::AbstractQuasiCyclicCode)

Return the circulant matrices of the quasi-cyclic code.
"""
function circulants(C::AbstractQuasiCyclicCode)
    circs = Vector{typeof(identity_matrix(C.F, 1))}()
    nr, nc = size(C.A)
    for r in 1:nr
        for c in 1:nc
            push!(circs, _poly_to_circulant(lift(C.A[r, c]), C.m, C.F))
        end
    end
    return circs
end

"""
$(TYPEDSIGNATURES)

Return the integer shift matrix (exponent matrix) of the Quasi-Cyclic code.
# Notes
* If a circulant is a pure monomial `x^e`, its entry is `e`.
* If a circulant is the zero matrix, its entry is `-1`.
* Throws an error if the matrix contains polynomials with multiple terms.
"""
function shift_matrix(C::AbstractQuasiCyclicCode)
    nr, nc = size(C.A)
    E = fill(-1, nr, nc)
    
    for r in 1:nr
        for c in 1:nc
            poly = lift(C.A[r, c])
            iszero(poly) && continue
            
            coeffs = collect(coefficients(poly))
            count(!iszero, coeffs) == 1 || throw(ArgumentError("Circulant at ($r, $c) is not a simple shift (has multiple terms)."))
            
            # Find the degree of the single non-zero term
            E[r, c] = findfirst(!iszero, coeffs) - 1
        end
    end
    
    return E
end
exponent_matrix(C::AbstractQuasiCyclicCode) = shift_matrix(C)

"""
$(TYPEDSIGNATURES)

Return `true` if the quasi-cyclic code contains an algebraic 4-cycle, based on 
Fossorier's condition. Evaluates entirely in the polynomial domain.
"""
function has_algebraic_4_cycle(C::AbstractQuasiCyclicCode)
    E = shift_matrix(C)
    nr, nc = size(E)
    m = C.m
    
    # A 4-cycle exists if there is a 2x2 submatrix in the shift matrix
    # without any -1 (null) entries, such that the alternating sum of shifts is 0 mod m.
    for r1 in 1:(nr - 1)
        for r2 in (r1 + 1):nr
            for c1 in 1:(nc - 1)
                for c2 in (c1 + 1):nc
                    e11, e12 = E[r1, c1], E[r1, c2]
                    e21, e22 = E[r2, c1], E[r2, c2]
                    
                    if e11 != -1 && e12 != -1 && e21 != -1 && e22 != -1
                        if mod(e11 - e12 + e22 - e21, m) == 0
                            return true
                        end
                    end
                end
            end
        end
    end
    return false
end

"""
$(TYPEDSIGNATURES)

Return `true` if the quasi-cyclic code contains an algebraic cycle of exactly `target_length`, 
based on Fossorier's condition.
# Notes
* `target_length` must be an even integer `2j` (e.g., 4, 6, 8).
* Evaluates entirely in the polynomial domain using a DFS backtracking algorithm.
"""
function has_algebraic_cycle(C::AbstractQuasiCyclicCode, target_length::Int)
    iseven(target_length) && target_length >= 4 || throw(ArgumentError("Target length must be an even integer >= 4."))
    
    E = shift_matrix(C)
    nr, nc = size(E)
    m = C.m
    
    visited_rows = falses(nr)
    visited_cols = falses(nc)
    
    function dfs(curr_node::Int, start_row::Int, depth::Int, current_sum::Int, is_row::Bool)
        if depth == target_length
            return curr_node == start_row && mod(current_sum, m) == 0
        end
        
        if is_row
            r = curr_node
            visited_rows[r] = true
            for c in 1:nc
                if E[r, c] != -1 && !visited_cols[c]
                    if dfs(c, start_row, depth + 1, current_sum + E[r, c], false)
                        visited_rows[r] = false 
                        return true
                    end
                end
            end
            visited_rows[r] = false 
        else
            c = curr_node
            visited_cols[c] = true
            for r in 1:nr
                if E[r, c] != -1
                    if depth == target_length - 1
                        if r == start_row
                            if dfs(r, start_row, depth + 1, current_sum - E[r, c], true)
                                visited_cols[c] = false 
                                return true
                            end
                        end
                    else
                        if !visited_rows[r]
                            if dfs(r, start_row, depth + 1, current_sum - E[r, c], true)
                                visited_cols[c] = false 
                                return true
                            end
                        end
                    end
                end
            end
            visited_cols[c] = false 
        end
        return false
    end

    for start_row in 1:nr
        fill!(visited_rows, false)
        fill!(visited_cols, false)
        if dfs(start_row, start_row, 0, 0, true)
            return true
        end
    end
    
    return false
end

"""
$(TYPEDSIGNATURES)

Decompose the Quasi-Cyclic polynomial matrix into its Fourier/CRT components.
# Notes
* Returns a vector of tuples `(g, A_K)`, where `g` is an irreducible factor of `x^m - 1` 
  and `A_K` is the polynomial matrix evaluated in the extension field `F[x]/<g(x)>`.
* Bypasses the massive memory allocation of `lift(A)`.
"""
function component_matrices(C::AbstractQuasiCyclicCode)
    S = base_ring(C.R)
    F = base_ring(S)
    m = C.m
    
    gcd(m, Int(characteristic(F))) == 1 || @warn "m is not coprime to field characteristic; decomposition contains nilpotent elements."
    
    facs = factor(gen(S)^m - 1)
    components = []
    
    for (g, _) in facs
        K, _ = residue_ring(S, g)
        nr, nc = size(C.A)
        A_K = zero_matrix(K, nr, nc)
        
        for r in 1:nr
            for c in 1:nc
                A_K[r, c] = K(lift(C.A[r, c]))
            end
        end
        push!(components, (g, A_K))
    end
    
    return components
end

"""
$(TYPEDSIGNATURES)

Compute the exact dimension `k` of the Quasi-Cyclic code algebraically.
# Notes
* Uses the CRT decomposition to compute the rank over small extension fields.
* Executes in a fraction of the time of `rank(lift(A))`.
"""
function algebraic_dimension(C::AbstractQuasiCyclicCode)
    cache = getfield(C, :cache)
    if !haskey(cache, :algebraic_k)
        total_rank = 0
        comps = component_matrices(C)
        
        for (g, A_K) in comps
            ext_deg = degree(g)
            total_rank += ext_deg * rank(A_K)
        end
        
        if C.A_type == :H
            cache[:algebraic_k] = C.n - total_rank
        else
            cache[:algebraic_k] = total_rank
        end
    end
    
    return cache[:algebraic_k]
end